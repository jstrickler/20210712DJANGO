"""
URL Configuration for dolphins
"""
from django.urls import path
from . import views   # import views from app

app_name = "dolphins"

urlpatterns = [
    # add url patterns for the dolphins app here

    # Examples:
    # path('', views.home, name='home'),
    # path('thing', views.thing, name='thing'),
]
